import 'package:flutter/material.dart';

class EmptyContactScreen extends StatelessWidget {
  const EmptyContactScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Contact is empty');
  }
}
